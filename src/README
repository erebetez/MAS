Web application
 The example web application.
 
 
 To run it:
 $mvn jetty:run
 
 
 


CalculationServer
 - The calculation Server is a standalone server that can start excel applications (excel using the eat toolkit).
 - It also provides a clientside programm that can be used as a service task inside an activiti task. The service
   task is spring confiugred
   
 The server is build with: 
 $gradle distZip
 
 The client is a jar that can be uploaded to the repo: 
 $gradle uploadArchives
  
  
componens
 Collection of usefull classes.
 $gradle uploadArchives